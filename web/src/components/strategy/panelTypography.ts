/** Font for strategy-map detail panels. Resolves to the user's selected
 *  typeface (the same `--app-font` the rest of the app uses) so the
 *  Preferences font picker reaches these inline-styled panels too. */
export const PANEL_FONT = 'var(--app-font)'
