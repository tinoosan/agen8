package protocol

// ProtocolVersion is the current protocol version emitted by the app server.
const ProtocolVersion = "1.0"

// MinSupportedVersion is the minimum protocol version this client/server supports.
const MinSupportedVersion = "1.0"
